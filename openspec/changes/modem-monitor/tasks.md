## 1. Каркас проекта и хранение

- [x] 1.1 Создать структуру пакета Python и точку входа с запуском event loop; проверить, что приложение запускается и корректно завершается по сигналу
- [x] 1.2 Объявить зависимости (асинхронный последовательный порт, HTTP-сервер, клиент Telegram, экспортёр Prometheus); проверить, что установка окружения проходит без ошибок
- [x] 1.3 Реализовать модель настроек и загрузку `settings.json` с значениями по умолчанию; проверить тестами загрузку отсутствующего файла, файла с частичными данными и файла с неизвестными полями
- [x] 1.4 Реализовать атомарную перезапись `settings.json` (временный файл, `fsync`, `rename`) и права `0600`; проверить тестом, что при прерывании записи на диске остаётся полная прежняя или полная новая версия
- [x] 1.5 Реализовать проверку обязательных настроек при старте (пароль интерфейса, адресат администратора, маркер Telegram); проверить, что при их отсутствии приложение сообщает об ошибке конфигурации
- [x] 1.6 Реализовать запись `events.jsonl` построчным добавлением; проверить тестом, что запись события не изменяет ранее записанные строки
- [x] 1.7 Реализовать чтение истории от конца файла с ограничением количества записей и фильтром по идентификатору SIM; проверить тестом на файле с большим числом строк, что читается только конец файла и повреждённые строки пропускаются
- [x] 1.8 Реализовать внутреннюю шину событий, разделяющую формирование события и его потребителей (журнал, уведомления, метрики, интерфейс); проверить тестом, что сбой одного потребителя не мешает остальным

## 2. Обмен AT-командами

- [x] 2.1 Реализовать сессию порта с единственным читающим циклом и очередью команд, одна команда в работе; проверить тестом на заглушке порта, что две одновременно поставленные команды получают свои ответы, не смешивая их
- [x] 2.2 Реализовать разбор многострочных ответов с признаками успеха и ошибки, включая код ошибки; проверить тестами успешный ответ, ответ с ошибкой и ответ с кодом ошибки
- [x] 2.3 Реализовать классификацию незапрошенных сообщений и их направление на шину событий, включая случай прихода посреди ответа на команду; проверить тестом, что результат команды остаётся корректным, а событие доставлено
- [x] 2.4 Реализовать отключение эхо при инициализации порта и таймауты, задаваемые для каждой команды; проверить тестами истечение таймаута и продолжение обработки очереди
- [x] 2.5 Реализовать запрет команд отправки сообщений, набора номера и перехода в режим передачи данных на уровне сессии; проверить тестом, что такая команда отклоняется исключением и ни один байт не записан в порт, а команда завершения вызова при этом выполняется
- [x] 2.6 Реализовать маскирование PIN-кода в диагностических записях обмена; проверить тестом, что значение PIN-кода отсутствует в выводе диагностики
- [x] 2.7 Реализовать лестницу восстановления (повтор команды, переоткрытие порта, программный сброс, состояние неисправности); проверить тестами переход по всем ступеням на заглушке порта
- [x] 2.8 Реализовать распознавание ошибки отсутствия устройства при чтении как признака исчезновения модема; проверить тестом, что сессия завершается и сообщает об исчезновении

## 3. Поведение семейств модемов

- [x] 3.1 Определить контракт поведения семейства со всеми различающимися операциями из спецификации; проверить, что контракт покрывает выбор порта, инициализацию, уведомления, разбор незапрошенных сообщений, сигнал, регистрацию, оператора, остаток попыток PIN, память сообщений и меры восстановления
- [x] 3.2 Реализовать универсальное AT-поведение как основу и как запасной вариант для неопознанных модемов; проверить тестами на записанных ответах модема
- [x] 3.3 Реализовать поведение семейства Huawei: выбор управляющего порта из нескольких, свои незапрошенные сообщения, чтение остатка попыток PIN, собственная команда сброса; проверить тестами на записанных ответах, включая ответ, из которого остаток попыток разобрать невозможно
- [x] 3.4 Реализовать поведение семейства SIM800: единственный порт, перебор скоростей, чтение остатка попыток PIN своей командой, опрос уровня сигнала; проверить тестами на записанных ответах
- [x] 3.5 Реализовать нормализацию уровня сигнала к дБм с отдельным представлением неизвестного значения; проверить тестами границы шкалы и значение «неизвестно»
- [x] 3.6 Реализовать реестр семейств и выбор поведения по ответу на пробу с использованием пары идентификаторов USB только как подсказки; проверить тестом, что модуль за универсальным мостом опознаётся по ответу модема, а не по идентификаторам моста

## 4. Обнаружение модемов

- [x] 4.1 Реализовать перечисление портов по списку драйверов и группировку по родительскому USB-устройству; проверить тестом на подготовленном дереве файлов, что многопортовое устройство даёт одну запись, а однопортовые — по одной каждое
- [x] 4.2 Реализовать пробу устройства с определением семейства, перебором скоростей и негативным кэшем для устройств, не отвечающих на AT; проверить тестами опознанное устройство, неопознанное отвечающее устройство и молчащее устройство
- [x] 4.3 Реализовать выбор управляющего порта опытным путём с переводом модема в состояние неисправности, если пригодного порта нет; проверить тестом на устройстве с тремя портами, из которых отвечает не первый
- [x] 4.4 Реализовать цикл сверки фактических устройств с обслуживаемыми, запуск и остановку обслуживания; проверить тестом появление и исчезновение устройства между циклами
- [x] 4.5 Реализовать дебаунс перед признанием модема исчезнувшим и счётчик переподключений; проверить тестом, что кратковременное исчезновение не даёт события пропадания, а счётчик увеличивается
- [x] 4.6 Обеспечить изоляцию сбоев: неисправность одного модема не влияет на остальные, для неисправного выполняются периодические попытки восстановления; проверить тестом с несколькими модемами, один из которых постоянно неисправен

## 5. Идентификация SIM и защита PIN-кода

- [x] 5.1 Реализовать чтение ICCID и IMEI и связывание модема с SIM в памяти; проверить тестом, что связь восстанавливается после перезапуска обслуживания модема без чтения файлов состояния
- [x] 5.2 Реализовать формирование автоматического имени SIM из ICCID с кодом страны и последними цифрами, и приоритет пользовательского имени; проверить тестами оба случая
- [x] 5.3 Реализовать проверку контрольной цифры ICCID как предупреждения, не блокирующего обслуживание; проверить тестами корректный и некорректный идентификатор
- [x] 5.4 Реализовать определение состояний SIM (отсутствует, требуется PIN, требуется PUK, готова) и фиксацию перехода состояний в журнал; проверить тестами все состояния на заглушке модема
- [x] 5.5 Реализовать защиту от блокировки по PUK: попытка ввода PIN только при остатке три и более, отказ при остатке два и менее и при неизвестном остатке; проверить тестами все ветви, включая случай неразборчивого ответа модема
- [x] 5.6 Реализовать однократность попытки, повторное чтение остатка после неудачи и отсутствие дальнейших попыток; проверить тестом, что после неверного PIN и перезапуска обслуживания попытка не повторяется, поскольку остаток стал равен двум
- [x] 5.7 Реализовать обработку состояния блокировки по PUK без отправки модему PIN и PUK; проверить тестом, что в порт не записано ни одной команды разблокировки
- [x] 5.8 Реализовать обнаружение SIM, отсутствующей в настройках, с формированием события для администратора; проверить тестом, что событие содержит автоматическое имя карты

## 6. Приём SMS

- [x] 6.1 Реализовать разбор двоичного представления сообщения: отправитель, время с часовым поясом, кодировка, служебный заголовок пользовательских данных; проверить тестами на наборе записанных сообщений обеих кодировок
- [x] 6.2 Реализовать распаковку семибитной кодировки с учётом смещения после служебного заголовка и последовательностей расширения; проверить тестом, что сообщение с символом из таблицы расширения декодируется верно
- [x] 6.3 Реализовать инициализацию приёма: двоичный режим, включение уведомлений о поступлении, выгребание уже накопившихся сообщений; проверить тестом, что сообщения из памяти обрабатываются при старте и память освобождается
- [x] 6.4 Реализовать чтение сообщения по уведомлению и немедленное удаление из памяти до сборки и уведомления; проверить тестом порядок операций
- [x] 6.5 Реализовать сборку многочастных сообщений объединением необработанных данных в порядке номеров частей с однократным декодированием; проверить тестами сообщение с символом, разрезанным границей частей, в обеих кодировках, включая символ из суррогатной пары
- [x] 6.6 Реализовать ключ группы сборки по отправителю, признаку объединения и числу частей, ограничение времени ожидания и игнорирование повторной части; проверить тестами переиспользование признака объединения и повторную доставку части
- [x] 6.7 Реализовать формирование события неполного сообщения по истечении ожидания с перечислением отсутствующих частей; проверить тестом, что событие помечено как неполное и содержит доступный текст
- [x] 6.8 Реализовать включение необработанных данных всех частей в запись журнала о сообщении; проверить тестом, что по записи журнала текст можно получить повторно
- [x] 6.9 Реализовать чтение заполненности памяти сообщений в регулярном опросе; проверить тестом, что значение попадает в состояние модема и в метрики

## 7. Входящие вызовы

- [x] 7.1 Реализовать включение передачи номера вызывающего в последовательности инициализации; проверить тестом наличие команды в инициализации для обоих семейств
- [x] 7.2 Реализовать окно ожидания сведений о номере после сигнала о вызове с формированием события по получении номера или по истечении ожидания; проверить тестами получение номера, отсутствие номера и скрытый номер
- [x] 7.3 Реализовать объединение повторяющихся сигналов одного вызова в одно событие и отделение следующего вызова; проверить тестом, что серия повторяющихся сигналов даёт ровно одно событие
- [x] 7.4 Реализовать отклонение вызова после закрытия окна ожидания с обработкой случая, когда вызывающий отключился первым, и случая неудачи отклонения; проверить тестами все три исхода
- [x] 7.5 Обеспечить наличие идентификатора SIM, её имени и идентификатора модема в событии вызова; проверить тестом состав события

## 8. Уведомления в Telegram

- [x] 8.1 Реализовать маршрутизацию событий: по адресату SIM для сообщений и вызовов, в адресат администратора для событий оборудования и для SIM без назначенного адресата; проверить тестами все ветви маршрутизации
- [x] 8.2 Реализовать очередь доставки с повторами и возрастающей задержкой, не влияющую на приём сообщений; проверить тестом, что при недоступности Telegram событие записано в журнал, память SIM освобождена, а уведомление остаётся в очереди
- [x] 8.3 Реализовать дедупликацию уведомлений о продолжительных состояниях и уведомление о восстановлении; проверить тестом, что многократный опрос в одном состоянии даёт одно уведомление, а выход из состояния даёт уведомление о восстановлении
- [x] 8.4 Реализовать форматирование уведомлений о сообщении, неполном сообщении и вызове с именем SIM, номером и временем; проверить тестами содержимое каждого вида уведомления
- [x] 8.5 Обеспечить отсутствие PIN-кода, пароля интерфейса и маркера Telegram в текстах уведомлений; проверить тестом уведомление о срабатывании защиты PIN-кода
- [x] 8.6 Обеспечить запись события в журнал до попытки отправки уведомления; проверить тестом, что при неудаче отправки запись присутствует

## 9. Выбор оператора

- [x] 9.1 Реализовать применение принудительного выбора сети при заданном операторе и автоматический выбор при незаданном; проверить тестами оба случая
- [x] 9.2 Реализовать применение изменения предпочитаемого оператора без перезапуска приложения; проверить тестом, что для присутствующего модема выбор применяется после сохранения настроек
- [x] 9.3 Реализовать поиск доступных сетей как отдельную операцию с собственным состоянием модема, увеличенным таймаутом и записью результата в журнал; проверить тестом, что поиск не выполняется в регулярном опросе
- [x] 9.4 Реализовать раздельное чтение состояния регистрации для голоса с сообщениями и для передачи данных, с признаком роуминга; проверить тестами частичную и полную регистрацию
- [x] 9.5 Реализовать контроль длительного отсутствия регистрации при заданном операторе с уведомлением администратора и без автоматического переключения на автоматический выбор; проверить тестом, что принудительный выбор остаётся в силе

## 10. HTTP-интерфейс

- [x] 10.1 Реализовать HTTP-сервер в том же процессе, не блокирующий опрос модемов; проверить тестом, что запросы обслуживаются во время длительной операции с модемом
- [x] 10.2 Реализовать авторизацию одним паролем для всех эндпоинтов, кроме метрик; проверить тестами отказ без авторизации, доступ с верным паролем и открытость эндпоинта метрик
- [x] 10.3 Реализовать программный интерфейс состояния модемов и SIM-карт; проверить тестами состав ответа для работающего модема, модема без SIM и неисправного модема, включая представление неизвестного уровня сигнала
- [x] 10.4 Реализовать программный интерфейс настроек SIM с PIN-кодом только для записи и сохранением прежнего значения при отсутствии поля в запросе; проверить тестами задание, удаление, отсутствие поля и отсутствие значения в любом ответе
- [x] 10.5 Реализовать программный интерфейс истории с фильтром по SIM и ограничением количества записей; проверить тестами общую историю, историю одной SIM и случай, когда записей меньше запрошенного
- [x] 10.6 Реализовать запуск поиска операторов из интерфейса и предложение выбрать оператора из результата; проверить тестом, что до завершения поиска модем отображается в состоянии поиска
- [x] 10.7 Реализовать страницу статуса с отображением всех требуемых сведений и обновлением без действий пользователя; проверить в браузере на работающем приложении, что изменение состояния модема отражается на открытой странице
- [x] 10.8 Реализовать страницу истории с пометкой неполных сообщений и страницу настроек SIM со скрытым PIN-кодом; проверить в браузере, что значение PIN-кода не отображается и не присутствует в исходном коде страницы

## 11. Метрики

- [x] 11.1 Реализовать эндпоинт метрик в формате Prometheus без авторизации; проверить, что вывод принимается парсером формата
- [x] 11.2 Реализовать метрики состояния и присутствия модема, уровня сигнала, состояния регистрации с оператором и роумингом, заполненности памяти сообщений и остатка попыток PIN; проверить тестами наличие метрик и их меток
- [x] 11.3 Реализовать счётчики полученных сообщений, отклонённых вызовов, ошибок обмена, переподключений модема и неудачных отправок уведомлений; проверить тестами увеличение счётчиков при соответствующих событиях
- [x] 11.4 Реализовать метрики времени последнего полученного сообщения и времени последнего успешного опроса модема; проверить тестом, что значения обновляются
- [x] 11.5 Реализовать информационную метрику с изменяемыми сведениями о SIM и оставить в метриках значений только неизменяемый идентификатор; проверить тестом, что переименование SIM не изменяет метки метрик значений
- [x] 11.6 Обеспечить отсутствие текстов сообщений, PIN-кодов, пароля и маркера Telegram в выводе метрик; проверить тестом по всему выводу эндпоинта

## 12. Развёртывание

- [x] 12.1 Описать в README назначение приложения, формат `settings.json`, обязательные настройки и порядок первичной настройки; проверить, что по README можно поднять приложение с нуля

## 13. Проверка на живом оборудовании

- [ ] 13.1 Проверить на подключённом Huawei обнаружение, выбор управляющего порта и опрос состояния; зафиксировать фактический формат ответа с остатком попыток PIN и при необходимости уточнить разбор
- [ ] 13.2 Проверить на подключённом SIM800 обнаружение через универсальный мост, определение скорости и опрос состояния; зафиксировать фактические имена хранилищ сообщений
- [ ] 13.3 Проверить приём длинного сообщения и сообщения со специальными символами на обоих семействах; сверить декодированный текст с текстом, полученным на телефоне
- [ ] 13.4 Проверить приём и отклонение входящего вызова на обоих семействах; убедиться, что номер вызывающего определяется и что один вызов даёт одно уведомление
- [ ] 13.5 Подобрать интервалы опроса и время дебаунса исчезновения по поведению реальных модемов; зафиксировать выбранные значения в настройках по умолчанию
- [ ] 13.6 Проверить сценарий заполненной памяти сообщений: заполнить память, убедиться, что приложение выгребает её при старте и что заполненность видна в интерфейсе и метриках
